# Claude Power UI — Netlify Deploy Configuration
# ────────────────────────────────────────────────────────────────
# No build step — pure static HTML/CSS/JS.
# Both /public/ (marketing site) and /app/ (AI workspace) are served.
# The Python server.py is for local use only and is not deployed.
#
# /api/proxy is handled by netlify/functions/proxy.mjs (AI proxy).
# All other /api/* calls (ping, state, sync) only work locally.
# ────────────────────────────────────────────────────────────────

[build]
  publish = "."
  ignore  = "exit 0"


# ── Security Headers ────────────────────────────────────────────

[[headers]]
  for = "/*"
  [headers.values]
    X-Frame-Options           = "DENY"
    X-Content-Type-Options    = "nosniff"
    Referrer-Policy           = "strict-origin-when-cross-origin"
    Permissions-Policy        = "camera=(), microphone=(), geolocation=()"

[[headers]]
  for = "/*.html"
  [headers.values]
    Cache-Control = "no-store, no-cache, must-revalidate"
    Content-Security-Policy = "frame-ancestors 'none'; default-src 'self'; script-src 'self' 'unsafe-inline' https://unpkg.com; style-src 'self' 'unsafe-inline' https://fonts.googleapis.com; style-src-elem 'self' 'unsafe-inline' https://fonts.googleapis.com; font-src 'self' https://fonts.gstatic.com data:; connect-src 'self' https://api.anthropic.com https://api.openai.com https://generativelanguage.googleapis.com https://api.groq.com https://api.mistral.ai https://unpkg.com; frame-src blob:; img-src 'self' data: blob:;"

[[headers]]
  for = "/*.css"
  [headers.values]
    Cache-Control = "public, max-age=31536000, immutable"

[[headers]]
  for = "/*.js"
  [headers.values]
    Cache-Control = "public, max-age=31536000, immutable"

# ── Routing / Redirects ─────────────────────────────────────────

# Root → public marketing homepage (200 rewrite, not redirect)
[[redirects]]
  from   = "/"
  to     = "/public/index.html"
  status = 200

# /app with no trailing slash
[[redirects]]
  from   = "/app"
  to     = "/app/index.html"
  status = 200

# /app/ SPA fallback
# Note: Netlify serves actual files first, so /app/styles.css etc. are unaffected
[[redirects]]
  from   = "/app/*"
  to     = "/app/index.html"
  status = 200

# /api/proxy → Netlify Function (alias so /api/proxy also works)
[[redirects]]
  from  = "/api/proxy"
  to    = "/.netlify/functions/proxy"
  status = 200
  force  = true

[[redirects]]
  from   = "/api/ping"
  to     = "/api/not-available.json"
  status = 404
  force  = false

[[redirects]]
  from   = "/api/state"
  to     = "/api/not-available.json"
  status = 404
  force  = false

[[redirects]]
  from   = "/api/sync"
  to     = "/api/not-available.json"
  status = 404
  force  = false

[[redirects]]
  from   = "/api/mcp/*"
  to     = "/api/not-available.json"
  status = 404
  force  = false
